const sum = (num1, num2) => {
    let result = num1 + num2
    return result;
}

export const minus = (num1, num2) => {
    let result = num1 - num2
    return result;
}

export const user = {
    name: "Test",
    id: 5
}

export default sum;